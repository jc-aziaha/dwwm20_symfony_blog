{% extends "themes/base_admin.html.twig" %}

{% block title %}Mon profil{% endblock %}

{% block description %}Mon profil{% endblock %}

{% block main %}
    <h1 class="text-center my-3 display-5">Mon profil</h1>

    {% include 'components/alert.html.twig' %}

    <div class="container mt-5">
        <div class="row">
            <div class="col-md-6 mx-auto shadow p-4 bg-light">
                <div>
                    <p><strong>Prénom</strong>: {{admin.firstName}}</p>
                    <p><strong>Nom</strong>: {{admin.lastName}}</p>
                    <p><strong>Email</strong>: {{admin.email}}</p>
                    <div>
                        <a href="{{path('app_admin_profile_edit')}}" class="btn btn-sm btn-info">Modifier le profil</a>
                        <a href="{{path('app_admin_profile_edit_password')}}" class="btn btn-sm btn-secondary">Modifier le mot de passe</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}