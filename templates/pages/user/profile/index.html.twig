{% extends "themes/base_user.html.twig" %}

{% block title %}Mon profil{% endblock %}

{% block description %}Mon profil{% endblock %}

{% block main %}
    <h1 class="text-center my-3 display-5">Mon profil</h1>

    {% include 'components/alert.html.twig' %}

    <div class="container mt-5">
        <div class="row">
            <div class="col-md-6 mx-auto shadow p-4 bg-light">
                <div>
                    <p><strong>Prénom</strong>: {{user.firstName}}</p>
                    <p><strong>Nom</strong>: {{user.lastName}}</p>
                    <p><strong>Email</strong>: {{user.email}}</p>
                    <div>
                        <a href="{{path('app_user_profile_edit')}}" class="btn btn-sm btn-info">Modifier le profil</a>
                        <a href="{{path('app_user_profile_edit_password')}}" class="btn btn-sm btn-secondary">Modifier le mot de passe</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}